---
name: cli-contexts
description: Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Manage context engineering configurations, RTK filter sets, and conversation sessions from the CLI. Apply context-relay settings and inspect active context pipelines.

## Quick install

```bash
npm install -g aera-router   # or: npx aera-router
aera-router --version
```

## Subcommands

### `context-eng`

**Example:**

```bash
aera-router context-eng
```

### `context-eng analytics`

**Flags:**

- `--period <p>`

**Example:**

```bash
aera-router context-eng analytics
```

### `context-eng caveman`

**Example:**

```bash
aera-router context-eng caveman
```

### `context-eng config`

**Example:**

```bash
aera-router context-eng config
```

### `context-eng show`

**Example:**

```bash
aera-router context-eng show
```

### `context-eng set`

**Flags:**

- `--aggressiveness <n>`
- `--max-shrink-pct <n>`
- `--preserve-tags <list>`

**Example:**

```bash
aera-router context-eng set
```

### `context-eng rtk`

**Example:**

```bash
aera-router context-eng rtk
```

### `context-eng config`

**Example:**

```bash
aera-router context-eng config
```

### `context-eng show`

**Example:**

```bash
aera-router context-eng show
```

### `context-eng set`

**Flags:**

- `--token-budget <n>`
- `--reserve-pct <n>`

**Example:**

```bash
aera-router context-eng set
```

### `context-eng filters`

**Example:**

```bash
aera-router context-eng filters
```

### `context-eng list`

**Example:**

```bash
aera-router context-eng list
```

### `context-eng add`

**Flags:**

- `--priority <n>`
- `--action <a>`

**Example:**

```bash
aera-router context-eng add
```

### `context-eng remove <id>`

**Flags:**

- `--yes`

**Example:**

```bash
aera-router context-eng remove <id>
```

### `context-eng test`

**Example:**

```bash
aera-router context-eng test
```

### `context-eng raw-output <id>`

**Example:**

```bash
aera-router context-eng raw-output <id>
```

### `context-eng combos`

**Example:**

```bash
aera-router context-eng combos
```

### `context-eng list`

**Example:**

```bash
aera-router context-eng list
```

### `context-eng get <id>`

**Example:**

```bash
aera-router context-eng get <id>
```

### `context-eng assignments <id>`

**Example:**

```bash
aera-router context-eng assignments <id>
```

### `contexts`

**Example:**

```bash
aera-router contexts
```

### `contexts list`

List all contexts

**Example:**

```bash
aera-router contexts list
```

### `contexts add <name>`

Add a new context

**Flags:**

- `--api-key <k>`
- `--api-key-stdin`
- `--access-token <t>`
- `--access-token-stdin`
- `--scope <s>`
- `--description <d>`

**Example:**

```bash
aera-router contexts add <name>
```

### `contexts use <name>`

Switch active context

**Example:**

```bash
aera-router contexts use <name>
```

### `contexts current`

Show the active context (server, auth, scope)

**Flags:**

- `--name-only`

**Example:**

```bash
aera-router contexts current
```

### `contexts show <name>`

Show context details

**Example:**

```bash
aera-router contexts show <name>
```

### `contexts remove <name>`

Remove a context

**Flags:**

- `--yes`

**Example:**

```bash
aera-router contexts remove <name>
```

### `contexts rename <old> <new>`

Rename a context

**Example:**

```bash
aera-router contexts rename <old> <new>
```

### `contexts export`

Export contexts to JSON

**Flags:**

- `--out <path>`
- `--no-secrets`

**Example:**

```bash
aera-router contexts export
```

### `contexts import <file>`

Import contexts from a JSON file

**Flags:**

- `--merge`

**Example:**

```bash
aera-router contexts import <file>
```

### `sessions`

**Example:**

```bash
aera-router sessions
```

### `sessions list`

**Flags:**

- `--user <u>`
- `--kind <k>`
- `--active`
- `--limit <n>`

**Example:**

```bash
aera-router sessions list
```

### `sessions show <sessionId>`

**Example:**

```bash
aera-router sessions show <sessionId>
```

### `sessions expire <sessionId>`

**Flags:**

- `--yes`

**Example:**

```bash
aera-router sessions expire <sessionId>
```

### `sessions expire-all`

**Flags:**

- `--yes`

**Example:**

```bash
aera-router sessions expire-all
```

### `sessions current`

**Example:**

```bash
aera-router sessions current
```
