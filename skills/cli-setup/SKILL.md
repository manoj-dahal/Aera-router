---
name: cli-setup
description: Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Run initial setup, configure global CLI settings, manage environment variables, check for updates, and configure autostart via the CLI setup and config commands.

## Quick install

```bash
npm install -g aera-router   # or: npx aera-router
aera-router --version
```

## Subcommands

### `autostart`

**Example:**

```bash
aera-router autostart
```

### `autostart enable`

**Example:**

```bash
aera-router autostart enable
```

### `autostart disable`

**Example:**

```bash
aera-router autostart disable
```

### `autostart toggle`

**Example:**

```bash
aera-router autostart toggle
```

### `autostart status`

**Example:**

```bash
aera-router autostart status
```

### `config`

Show or update CLI tool configuration

**Example:**

```bash
aera-router config
```

### `config list`

List all CLI tools and config status

**Flags:**

- `--json`

**Example:**

```bash
aera-router config list
```

### `config get <tool>`

Show current config for a tool

**Flags:**

- `--json`

**Example:**

```bash
aera-router config get <tool>
```

### `config set <tool>`

Write config for a tool

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
aera-router config set <tool>
```

### `config validate <tool>`

Validate config format without writing

**Flags:**

- `--model <model>`
- `--json`

**Example:**

```bash
aera-router config validate <tool>
```

### `config opencode`

Generate OpenCode config (alias for

**Flags:**

- `--model <model>`
- `--non-interactive`
- `--yes`

**Example:**

```bash
aera-router config opencode
```

### `config lang`

**Example:**

```bash
aera-router config lang
```

### `config get`

**Flags:**

- `--json`

**Example:**

```bash
aera-router config get
```

### `config set <code>`

**Flags:**

- `--force`

**Example:**

```bash
aera-router config set <code>
```

### `config list`

**Flags:**

- `--json`

**Example:**

```bash
aera-router config list
```

### `env`

Show and manage environment variables

**Example:**

```bash
aera-router env
```

### `env show`

Show current environment variables

**Flags:**

- `--json`

**Example:**

```bash
aera-router env show
```

### `env get <key>`

Get a single environment variable

**Example:**

```bash
aera-router env get <key>
```

### `env set <key> <value>`

Set an environment variable (current session only)

**Example:**

```bash
aera-router env set <key> <value>
```

### `setup`

**Flags:**

- `--password <value>`
- `--add-provider`
- `--provider <id>`
- `--provider-name <name>`
- `--api-key <value>`
- `--default-model <model>`
- `--provider-base-url <url>`
- `--test-provider`
- `--non-interactive`
- `--list`

**Example:**

```bash
aera-router setup
```

### `update`

**Flags:**

- `--check`
- `--apply`
- `--changelog`
- `--dry-run`
- `--no-backup`
- `--yes`

**Example:**

```bash
aera-router update
```
