---
name: cli-policy-audit
description: Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

Inspect audit logs, manage access policies, view telemetry data, and review request history from the CLI. Filter by event type, user, or time range for compliance workflows.

## Quick install

```bash
npm install -g aera-router   # or: npx aera-router
aera-router --version
```

## Subcommands

### `audit`

**Example:**

```bash
aera-router audit
```

### `audit tail`

**Flags:**

- `--source <s>`
- `--follow`
- `--limit <n>`

**Example:**

```bash
aera-router audit tail
```

### `audit search <query>`

**Flags:**

- `--source <s>`
- `--since <ts>`
- `--until <ts>`
- `--limit <n>`
- `--actor <id>`
- `--action <a>`

**Example:**

```bash
aera-router audit search <query>
```

### `audit export <file>`

**Flags:**

- `--source <s>`
- `--format <f>`
- `--since <ts>`
- `--until <ts>`

**Example:**

```bash
aera-router audit export <file>
```

### `audit stats`

**Flags:**

- `--source <s>`
- `--period <p>`

**Example:**

```bash
aera-router audit stats
```

### `audit get <id>`

**Flags:**

- `--source <s>`

**Example:**

```bash
aera-router audit get <id>
```

### `logs`

**Flags:**

- `--follow`
- `--filter <level>`
- `--lines <n>`
- `--timeout <ms>`
- `--base-url <url>`
- `--request-id <id>`
- `--api-key <key>`
- `--combo <name>`
- `--status <code>`
- `--duration-min <ms>`
- `--duration-max <ms>`
- `--export <path>`

**Example:**

```bash
aera-router logs
```

### `policy`

**Example:**

```bash
aera-router policy
```

### `policy list`

**Flags:**

- `--kind <k>`
- `--scope <s>`

**Example:**

```bash
aera-router policy list
```

### `policy get <id>`

**Example:**

```bash
aera-router policy get <id>
```

### `policy create`

**Example:**

```bash
aera-router policy create
```

### `policy update <id>`

**Example:**

```bash
aera-router policy update <id>
```

### `policy delete <id>`

**Flags:**

- `--yes`

**Example:**

```bash
aera-router policy delete <id>
```

### `policy evaluate`

**Flags:**

- `--resource <r>`
- `--context <json>`

**Example:**

```bash
aera-router policy evaluate
```

### `policy export <file>`

**Example:**

```bash
aera-router policy export <file>
```

### `policy import <file>`

**Flags:**

- `--overwrite`

**Example:**

```bash
aera-router policy import <file>
```

### `telemetry`

**Example:**

```bash
aera-router telemetry
```

### `telemetry summary`

**Flags:**

- `--period <p>`
- `--compare-to <p>`

**Example:**

```bash
aera-router telemetry summary
```

### `telemetry export`

**Flags:**

- `--out <path>`
- `--period <p>`

**Example:**

```bash
aera-router telemetry export
```
