---
name: cli-cost-usage
description: View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.
---

<!-- generated by src/lib/agentSkills/generator.ts; manual edits will be overwritten -->

## Overview

View cost breakdowns, token usage, and call logs from the CLI. Filter by provider, model, or date range. Export usage reports and inspect per-connection spending.

## Quick install

```bash
npm install -g aera-router   # or: npx aera-router
aera-router --version
```

## Subcommands

### `cost`

**Flags:**

- `--period <range>`
- `--since <date>`
- `--until <date>`
- `--group-by <field>`
- `--api-key <key>`
- `--limit <n>`

**Example:**

```bash
aera-router cost
```

### `pricing`

**Example:**

```bash
aera-router pricing
```

### `pricing sync`

**Flags:**

- `--provider <p>`
- `--force`

**Example:**

```bash
aera-router pricing sync
```

### `pricing list`

**Flags:**

- `--provider <p>`
- `--model <m>`
- `--limit <n>`

**Example:**

```bash
aera-router pricing list
```

### `pricing get <model>`

**Example:**

```bash
aera-router pricing get <model>
```

### `pricing defaults`

**Example:**

```bash
aera-router pricing defaults
```

### `pricing show`

**Example:**

```bash
aera-router pricing show
```

### `pricing set`

**Flags:**

- `--input <p>`
- `--output <p>`
- `--cache-read <p>`
- `--cache-write <p>`

**Example:**

```bash
aera-router pricing set
```

### `pricing diff`

**Flags:**

- `--model <m>`

**Example:**

```bash
aera-router pricing diff
```

### `usage`

**Example:**

```bash
aera-router usage
```

### `usage analytics`

**Flags:**

- `--period <range>`
- `--provider <id>`

**Example:**

```bash
aera-router usage analytics
```

### `usage budget`

**Example:**

```bash
aera-router usage budget
```

### `usage list`

**Example:**

```bash
aera-router usage list
```

### `usage get [scope]`

**Example:**

```bash
aera-router usage get [scope]
```

### `usage set <amount>`

**Flags:**

- `--scope <s>`
- `--period <p>`

**Example:**

```bash
aera-router usage set <amount>
```

### `usage reset [scope]`

**Example:**

```bash
aera-router usage reset [scope]
```

### `usage quota`

**Flags:**

- `--provider <id>`
- `--check`

**Example:**

```bash
aera-router usage quota
```

### `usage logs`

**Flags:**

- `--limit <n>`
- `--search <q>`
- `--since <ts>`
- `--follow`
- `--api-key <k>`

**Example:**

```bash
aera-router usage logs
```

### `usage utilization`

**Flags:**

- `--api-key <k>`

**Example:**

```bash
aera-router usage utilization
```

### `usage history`

**Flags:**

- `--limit <n>`

**Example:**

```bash
aera-router usage history
```

### `usage proxy-logs`

**Flags:**

- `--limit <n>`

**Example:**

```bash
aera-router usage proxy-logs
```
