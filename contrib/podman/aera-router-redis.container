[Unit]
Description=Aera Router Redis (rate-limiter backend)
After=network-online.target
Wants=network-online.target

[Container]
Image=docker.io/redis:7-alpine
ContainerName=aera-router-redis

Network=aera-router.network
NetworkAlias=redis

Volume=aera-router-redis.volume:/data

Exec=redis-server --save 60 1 --loglevel warning

HealthCmd=/bin/sh -c "redis-cli ping | grep -q PONG"
HealthInterval=10s
HealthRetries=3
HealthTimeout=5s

[Service]
Restart=always

[Install]
WantedBy=default.target
